1. //int index;

        // connection string
        /* DataSet getAllNhanVien()
        {

            DataSet dts = new DataSet();
            // SqlConnection
            // sau khi sài sẽ ngắt kết nối
            string query = "select* from NhanVien";
            using (SqlConnection sqlconnect = new SqlConnection(ConnectionString.str))
            {
                sqlconnect.Open();
                SqlDataAdapter sqladapter = new SqlDataAdapter(query, sqlconnect);
                sqladapter.Fill(dts);
                sqlconnect.Close();
            }

            // SqlCommand
            // SqlDataAdapter
            return dts;
        }  */

cách kết nối cơ sở dữ liệu thứ hai dùng dataset